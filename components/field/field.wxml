<view class="m-field">
  <view class="m-field-header">
    <view class="m-field-info">
      <view class="m-field-title">{{title}}</view>
      <view class="m-field-brief" wx:if="{{brief}}">{{brief}}</view>
    </view>
  </view>
  <view class="m-field-body">
    <slot></slot>
  </view>
</view>